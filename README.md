# Marudham T S - Portfolio
Just an idea to create a single page application(which is also my personal portfolio) with unique design(first idea is to make it like a terminal, where to give controll to navigate only thought keys shortcuts), not like every other similar sites.
